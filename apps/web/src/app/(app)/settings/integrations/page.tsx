export const metadata = { title: "Integrations" };
export default function IntegrationsPage() {
  return (
    <p className="text-sm text-tertiary">
      Threads connection status, update token, verify (admin) — T-041. Tokens are never shown.
    </p>
  );
}
